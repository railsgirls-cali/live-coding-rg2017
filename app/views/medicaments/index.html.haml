%p#notice= notice
%h1 Medicaments
%table
  %thead
    %tr
      %th Name
      %th Description
      %th Brand
      %th Price
      %th Disease
      %th{:colspan => "3"}
  %tbody
    - @medicaments.each do |medicament|
      %tr
        %td= medicament.name
        %td= medicament.description
        %td= medicament.brand
        %td= medicament.price
        %td= medicament.disease
        %td= link_to 'Show', medicament
        %td= link_to 'Edit', edit_medicament_path(medicament)
        %td= link_to 'Destroy', medicament, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Medicament', new_medicament_path
