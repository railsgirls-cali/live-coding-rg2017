.col-md-9
  - @recipes.each do |recipe|
    %article{ style: 'margin-top: 24px' }
      %header
        .float-right
          = link_to 'Editar', edit_recipe_path(recipe)
          = link_to 'Borrar', recipe, method: :delete, data: { confirm: 'Are you sure?' }
        %h1= link_to recipe.name, recipe
        %h5
          TRATAMIENTO para:
          = recipe.disease.name
      %section.row
        .col-md-4
          %figure
            = image_tag recipe.picture, style: 'width:200px'
        .col-md-8
          %p= recipe.description
